import styled from "styled-components";

import { ScrollMenu } from "react-horizontal-scrolling-menu";

export const ScrollingMenu = styled(ScrollMenu)`
  display: flex;
  flex-wrap: wrap;
  width: 100%;
  padding: 10px;
`;
